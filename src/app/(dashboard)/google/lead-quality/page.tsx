"use client"

// Google Lead Quality — renders the shared LeadQualityView with Google platform
// URL: /google/lead-quality → usePlatform() returns "google" (matches /google/* rule)

export { default } from "../../lead-quality/page"
